# Disable directory browsing
Options -Indexes

# Set the default page
DirectoryIndex index.html index.php

# Redirect www to non-www
RewriteEngine On
RewriteCond %{HTTP_HOST} ^www\.capstone\.local [NC]
RewriteRule ^(.*)$ http://capstone.local/$1 [L,R=301]
